{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>sanity-io/renovate-config"],
  "packageRules": [
    {
      "matchManagers": ["npm"],
      "semanticCommitType": "chore"
    },
    {
      "group": {
        "semanticCommitType": "{{#if isLockfileUpdate}}{{#unless isRange}}fix{{else}}chore{{/unless}}{{else}}fix{{/if}}"
      },
      "matchDepTypes": [
        "dependencies",
        "engines",
        "optionalDependencies",
        "peerDependencies"
      ],
      "matchManagers": ["npm"],
      "semanticCommitType": "{{#if isLockfileUpdate}}chore{{else}}fix{{/if}}"
    }
  ],
  "ignorePresets": [":semanticPrefixFixDepsChoreOthers"]
}
