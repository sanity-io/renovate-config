{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Set semantic commit type based on advanced rules",
  "packageRules": [
    { "matchManagers": ["npm"], "semanticCommitType": "chore" },
    {
      "group": {
        "semanticCommitType": "{{#if isLockfileUpdate}}{{#if (or isRange isSingleVersion)}}fix{{else}}chore{{/if}}{{else}}fix{{/if}}",
        "semanticCommitScope": "{{#if isRange}}range{{#if isSingleVersion}}single-version{{#if isLockfileUpdate}}lockfile{{else}}deps{{/if}}{{/if}}{{/if}}"
      },
      "matchDepTypes": [
        "dependencies",
        "engines",
        "optionalDependencies",
        "peerDependencies"
      ],
      "matchManagers": ["npm"],
      "semanticCommitType": "{{#if isLockfileUpdate}}chore{{else}}fix{{/if}}",
      "semanticCommitScope": "{{#if isLockfileUpdate}}lockfile{{else}}deps{{/if}}"
    }
  ],
  "ignorePresets": [
    ":semanticPrefixFixDepsChoreOthers"
  ]
}
